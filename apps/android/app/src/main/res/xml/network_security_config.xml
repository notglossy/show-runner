<?xml version="1.0" encoding="utf-8"?>
<!-- The server runs on the LAN over plain HTTP for now. Tighten this once it's behind TLS. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
